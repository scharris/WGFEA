Try parameterizing polynomials by their domain dimension.

Create abstract type Mesh.

Finite-element shape/dimension specific functions should be in the particular Mesh impl. package.
  Examples:
    face types
    enumeration of faces other than outside boundary edges in the mesh
    integration functions on a face

AbstractMesh functions
  face_types(mesh)
  num_faces_of_type(face_num::FaceNum, mesh)
  face_type_rel_face_num_to_rc(rel_face_num, face_type, mesh)


